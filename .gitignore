# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
*.env
# smoke test screenshots
scripts/shots

# personal TV Time exports (test data)
*.zip

# local worker secrets
worker/.dev.vars
.wrangler/

.dev.vars*
!.dev.vars.example
!.env.example

# local-only files that must never be committed
.env.backup*
.claude/settings.local.json

# Release signing — the upload key and its passwords never go in the repo.
# keystore.properties.example documents the shape; the real one stays local.
android/keystore/
android/keystore.properties
