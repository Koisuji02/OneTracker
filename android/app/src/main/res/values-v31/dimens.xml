<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+: use the launcher's declared widget radii so OneTracker's
         widget has exactly the same rounding as the system/Google widgets -->
    <dimen name="widget_radius">@android:dimen/system_app_widget_background_radius</dimen>
    <dimen name="widget_inner_radius">@android:dimen/system_app_widget_inner_radius</dimen>
    <!-- one "Continue" row: the frame and, 1dp tighter, the card inside it -->
    <dimen name="widget_row_radius">@android:dimen/system_app_widget_inner_radius</dimen>
    <dimen name="widget_row_inner_radius">@android:dimen/system_app_widget_inner_radius</dimen>
</resources>
